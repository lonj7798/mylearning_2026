# 제45c장 — 장기 에이전트의 context 관리

긴 작업은 context window를 채우므로 관찰 요약, 오래된 메시지 제거, 외부 메모리, tool 결과 압축을 명시적으로 설계한다. 같은 task를 context 길이와 압축 방식만 바꿔 실행해 성공률, tool 재호출, 잘못된 인용, 토큰 비용을 측정한다. 요약이 원래 목표·제약·실패 정보를 잃으면 장기 성공률이 떨어진다.
