---
chapter: ch-29b
course: llm-training
phase: read
excerpt_of: wiki/raw-data/llm-training/blogs/openai-mrcr-graphwalks.md (planned card; not present on 2026-09-15)
source_url: https://huggingface.co/datasets/openai/mrcr
primary_version: openai/mrcr README at revision f4c69fa (last modified 2025-12-08)
created_at: "2026-09-15"
---

# Excerpt: OpenAI MRCR dataset card (multi-round co-reference resolution)

Organization: OpenAI (official dataset card, MIT license). Only the MRCR part is used by ch-29b; the Graphwalks card is quoted in ch-32c's excerpt of the same name.

## Task
- "The model is given a long, multi-turn, synthetically generated conversation between user and model where the user asks for a piece of writing about a topic." "Hidden in this conversation are 2, 4, or 8 identical asks, and the model is ultimately prompted to return the i-th instance of one of those asks."
- Final user turn in the 2-needle example: "Prepend aYooSG8CQg to the 2nd (1 indexed) poem about tapirs. Do not include any other text in your response."
- Difficulty sources listed by the card: "The needles are selected from the same distribution as the distractors. All assistant responses are generated by gpt4o"; the model must distinguish order among needles; more needles and longer context are harder.
- "Inspired by the MRCR eval first introduced by Gemini (arXiv:2409.12640v2)".

## Size and grading
- 438 distinct entities, 10 writing formats, 100 samples per bin; bins by prompt + answer tokens: [4096, 8192], (8192, 16384], ..., (524288, 1048576]. Token counts use tiktoken `o200k_base` in the reference script.
- Metric: `difflib.SequenceMatcher` ratio after stripping the required hash; a missing hash scores 0.

```python
def grade(response, answer, random_string_to_prepend) -> float:
    if not response.startswith(random_string_to_prepend):
        return 0
    response = response.removeprefix(random_string_to_prepend)
    answer = answer.removeprefix(random_string_to_prepend)
    return float(SequenceMatcher(None, response, answer).ratio())
```

## Changelog (data errors in a published generator)
- 12/5/2025: "A bug during generation caused ~10% of datapoints to contain too many target needles, and ~5% of datapoints to contain incorrect ground truth." Corrected rows carry a `date_added` field.
- Model results are referred to the GPT-4.1 release post, which was not read; ch-29b quotes no MRCR scores.
